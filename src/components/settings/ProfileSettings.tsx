const ProfileSettings = () => {
  return (
    <div>
      <h1 className="text-center text-xl font-extrabold sm:text-4xl">
        This settings tab is not yet implemented
      </h1>
      {/* TODO: Settings like: changing profile pic (after uploadthing), changing nickname, deleting account
       */}
    </div>
  );
};

export default ProfileSettings;
